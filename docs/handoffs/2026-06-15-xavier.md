# Handoff — 2026-06-15 — Xavier

> Handoff de arranque. Si eres mi cuate y empiezas a trabajar: lee esto completo + `docs/STATUS.md` +
> abre los 4 entregables en `docs/estrategia/`. Con eso tienes TODO mi contexto.

**Qué hice:**
- Definí y monté el **sistema de colaboración** de este repo: `CLAUDE.md` (reglas), slash commands
  `/catchup` y `/handoff`, hook de SessionStart que auto-carga el STATUS, plantilla de handoff,
  `STATUS.md` y `decisions.md`.
- Subí los **4 entregables de estrategia** que produje con Claude (en `docs/estrategia/`).
- Antes de esto: corrí varias investigaciones con agentes (Trainingym, mercado, marketing, conversión)
  que culminaron en el pivote y el objetivo #1.

**Por qué:**
- Trabajamos dos personas en paralelo y necesitábamos una forma de **llevar el mismo contexto** sin
  compartir la conversación de Claude (no se puede). La solución: el repo es el cerebro; handoffs
  diarios + STATUS vivo lo mantienen sincronizado.
- La estrategia pivoteó fuerte (ver abajo) y había que dejarla escrita para que ambos vendamos lo mismo.

**Estado actual (qué corre / qué falla):**
- **La narrativa del proyecto cambió.** Contexto para que entiendas el porqué:
  1. El plan original era un sistema enorme (15 agentes IA + plataforma de reservas + 12 funciones).
  2. El dueño nos dijo que **ya usa Trainingym** (SaaS de gestión de gym) que cubre varias cosas.
  3. **Pivote:** no reemplazar Trainingym → ser la **capa companion** (inteligencia + admin + crecimiento)
     encima. Solo 2 de 25 piezas de la propuesta original eran realmente redundantes con Trainingym.
  4. Investigación de mercado (verificada): Bahía es el **único club multideporte premium independiente**
     de la bahía (los demás son mono-deporte por hora vía Playtomic, o resorts cerrados). Boom de pádel
     (+1M en MX) y pickleball (deporte de los ~35-40k expats). Pricing aterrizado con benchmarks.
  5. **Objetivo #1 acordado: Motor de Conversión Day Pass → Inscripción** (ver entregable 04).
- Los 4 entregables HTML están en `docs/estrategia/` y abren bien en navegador (imprimibles a PDF).
- **Falta:** todo lo que depende del dueño (ver Bloqueos) y aterrizar el motor en un plan de ejecución.

**Siguiente paso:**
- Preparar la **checklist de la "Semana 0"**: la sesión de descubrimiento de datos con el dueño
  (qué exporta Trainingym, si hay acceso Playtomic, reglas de negocio del Day Pass). Es el verdadero
  primer paso antes de construir nada.
- Actualizar el deck `presentacion-agentes-bahia.html` al posicionamiento companion (hoy aún refleja
  el plan viejo / "no partimos de cero").

**Archivos tocados:**
- Nuevos: `CLAUDE.md`, `.claude/commands/{catchup,handoff}.md`, `.claude/settings.json`,
  `docs/STATUS.md`, `docs/decisions.md`, `docs/handoffs/_TEMPLATE.md`, este handoff,
  `docs/estrategia/0{1,2,3,4}-*.html`.
- NO toqué los archivos previos del repo (deck, sitio, briefing, comparación).

**Bloqueos / dudas para el otro:**
- Necesitamos del **dueño**: auditoría de Trainingym (con login), si usa Playtomic y cuánta demanda le
  trae, accesos a Meta (IG/FB) + WhatsApp Business API, y las reglas del Day Pass (precio temporada
  alta/baja, qué incluye, ventana de acreditación).
- Pon tu nombre en el bloque `## <Cuate>` de `docs/STATUS.md` cuando arranques.
- Duda abierta: ¿perseguimos el **partner program de Trainingym** (categoría Marketing/Gestión) o
  perfil bajo con solo exports Excel al inicio? (Lo dejé como decisión pendiente.)
