# Handoff — 2026-06-23 — Gonzalo

**Qué hice:**
- **Mejoré 6 agentes** de `bahia-agents`, cada uno con **memoria/entrenamiento** (aprenden de lo que funciona y dejan de inventar cuando no hay dato):
  - **Tendencias:** reporte completo — generación en **3 llamadas enfocadas** (brief · táctico · ideas) para que no se quede a medias; regla de honestidad (si una fuente no trae datos, lo dice); y **Google Trends ahora muestra los keywords reales** con su score (antes mostraba los inventados por Claude y se perdía el dato).
  - **Secretaria:** aprobación segura (lista y aprueba el creativo específico; rutea carruseles al ciclo de contenido), **búsqueda de leads por nombre** con su conversación, resumen proactivo, y **memoria total** (hilo + historial profundo de todos los agentes).
  - **Eventos:** **pósters de evento exportables a PNG** con identidad Bahía y foto REAL del club por deporte, **agenda histórica** (recurrentes/próximos/pasados), y memoria de instrucciones.
  - **Seguimiento:** **memoria/entrenamiento** (marca bueno/malo según si el lead avanzó, reutiliza lo que convirtió) + **preview de mensajes** (dryRun genera el texto sin enviar).
  - **Crítico:** **tendencia temporal** (dice si el sistema mejoró/empeoró vs antes) + honestidad de atribución (no inventa rendimiento por campaña si no hay enlace lead→campaña).
  - **Contenido:** horneé el método de las skills instaladas (**voz de marca + caption-writer + collage**) en los prompts; sin generación de imágenes IA (solo fotos del club).
- **Tema Bahía Deep** en el dashboard (paleta navy/oro/azul/sage + ballena de fondo + "BAH.IA").
- **Entregable nuevo:** documento HTML comparativo del **plan de marketing de Bahía** (Bahía Performance System™) — módulo por módulo, emparejando cada problema de hoy con su resultado directo, con notas de lo que se activa al conectar canales. En la raíz: `comparativo-evolucion-propuesta.html`.

**Por qué:**
- Cerrar el ciclo de mejoras internas: todo lo mejorable **sin conexiones externas** ya quedó. Cada agente ahora razona con memoria y es honesto.
- El comparativo es entregable de venta: muestra al cliente cómo mejoramos su plan actual, no uno genérico.

**Estado actual (qué corre / qué falla):**
- **Producción (`main`) al día**, CI verde, **0 PRs abiertos**. Los 6 agentes + tema desplegados.
- ⚠️ **Google Trends sale 0 / "sin datos":** falta poner `LINKFOXAGENT_API_KEY` en Vercel (el resto del reporte sí sale completo). Tras conectarla hay que generar un reporte nuevo.
- 🔌 **Requieren conexión externa** (no fallan, están listos): Ventas/Seguimiento **en vivo** → WhatsApp Business; **Meta Ads** → cuenta de Meta; **Reputación** → Google Business.

**Siguiente paso:**
- Poner `LINKFOXAGENT_API_KEY` en Vercel (registro en skill.linkfox.com — la página está en chino) → Google Trends reales.
- Conectar los canales para encender lo que está gated (WhatsApp, Meta, Google).
- **Meter el agente de Ventas en Instagram + Messenger** — integración nueva acotada (clonar el patrón del webhook de WhatsApp). Camino técnico guardado en memoria.

**Archivos tocados:**
- `bahia-agents`: `app/api/agents/{tendencias,secretaria,eventos,seguimiento,critico,contenido}/route.ts`, sus páginas en `app/dashboard/...`, `app/globals.css`, `app/dashboard/_components/{ui.tsx,Sidebar.tsx}`, `vercel.json`, `package.json` (html-to-image). PRs #31–#43, todo en `main`.
- Raíz (este repo): `comparativo-evolucion-propuesta.html` (nuevo).

**Bloqueos / dudas para Xavier:**
- **LinkFox key:** el registro es en chino; pendiente de conseguir y poner en Vercel.
- **Canales IG/FB para Ventas:** integración nueva — webhooks de Instagram Messaging + Messenger Platform (Graph API) + adaptador de envío, y **revisión de la app por Meta** (tarda días). Ojo con la ventana de 24h y la deduplicación de identidad (IG ≠ WhatsApp).
- ¿El harness (`AgentRunLog`) ya registra **costo por corrida**? Sería útil para que Crítico/Secretaria reporten cuánto van gastando los agentes.
